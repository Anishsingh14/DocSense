# --- Backend API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
python-dotenv==1.0.1

# --- Document parsing ---
pymupdf==1.24.10          # PDF text + image extraction (PyMuPDF / fitz)
python-docx==1.1.2        # DOCX parsing
pytesseract==0.3.13       # OCR for scanned PDFs (requires Tesseract installed on system)
pdf2image==1.17.0         # Converts PDF pages to images for OCR
Pillow==10.4.0             # Image handling

# --- Chunking / Orchestration ---
llama-index==0.11.14
llama-index-embeddings-voyageai==0.2.3
llama-index-vector-stores-qdrant==0.3.0

# --- Embeddings ---
# voyageai==0.2.3          # Voyage AI embeddings (paid API) — per TDR.md, not currently used.
                           # DEVIATION: see Section 2a. Gemini embedding API (gemini-embedding-001)
                           # is used instead for Stage 4 (embedding/embed_chunks.py).
sentence-transformers==3.1.1   # Local/free fallback (BGE models) — available if Gemini
                                # embeddings are ever unavailable/rate-limited

# --- Vector database ---
qdrant-client==1.11.2

# --- Re-ranking ---
FlagEmbedding==1.2.11      # bge-reranker-v2-m3

# --- LLM / Vision ---
anthropic==0.34.2          # Claude API (generation + vision) — per TDR.md, not currently used.
                           # DEVIATION: user has no Claude/Anthropic billing set up. Stages 3
                           # (vision) and 6 (generation) use Gemini instead (see below). Kept
                           # here, unpinned from active use, so switching back later is a small
                           # change (swap the client in image_analyzer.py / answer_with_citations.py).
google-genai==2.24.0       # Google Gemini API — vision (Stage 3), embeddings (Stage 4), and
                           # generation (Stage 6): substitute for Claude (vision/generation) and
                           # Voyage AI (embeddings)


# --- Evaluation ---
ragas==0.1.20
datasets==3.0.1

# --- Frontend (Stage 9 only) ---
streamlit==1.39.0

# --- Utilities ---
numpy==1.26.4
pandas==2.2.3
tqdm==4.66.5
